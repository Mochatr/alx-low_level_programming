#include "main.h"

/**
 * _abs - Computes absolute value of a number
 * @n: the number to check
 *
 * Return: Absolute value of int
 */

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
		return (-n);
}
